<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
    <title>Verification</title>

    <!-- Link to CSS (served via Express static) -->
    <link rel="stylesheet" href="/css/verify.css" />

    <!-- Link to JavaScript (deferred for better performance) -->
    <script src="/JavaScript/All.js" defer></script>
</head>
<body>

<div class="container">
    <div id="emailVerificationSection">
        <h3>Enter the Code Sent to Your Email</h3>
        <input 
            type="text" 
            id="emailCode" 
            placeholder="Enter verification code" 
            required 
        />
        <input 
            type="button" 
            value="Verify" 
            id="verifyButton" 
            onclick="verifyEmailCode()" 
        />
    </div>
    
    <div class="message" id="message"></div>
    <div class="error" id="errorMessage"></div>
</div>

</body>
</html>
