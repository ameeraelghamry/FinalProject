<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Bookings</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="/css/Admin/rented.css" />
 <script>
  function redirectToDetails(bookingId) {
    window.location.href = `/admin/bookings/${bookingId}`;
  }
</script>

</head>
<body>

<nav class="navbar navbar-expand-lg fixed-top">
  <a class="navbar-brand text-white" href="/admin/home" style="padding-left: 20px;">NileWay</a>
  <div class="collapse navbar-collapse justify-content-center">
    <ul class="navbar-nav">
      <li class="nav-item active px-4"><a class="nav-link text-white" href="#">Inventory</a></li>
      <li class="nav-item active px-4"><a class="nav-link text-white" href="#">Status</a></li>
      <li class="nav-item active px-4"><a class="nav-link text-white" href="#">Bookings</a></li>
    </ul>
  </div>
  <form class="d-flex me-4">
    <input class="form-control me-2" type="search" placeholder="Search" aria-label="Search">
    <button class="btn btn-outline-light" type="submit">Search</button>
  </form>
</nav>

<div class="container mt-5 pt-4">
  <div class="row">
    <% bookings.forEach(booking => { %>
      <div class="col-12 card p-3 mb-3" onclick="redirectToDetails('<%= booking._id %>')" style="cursor: pointer;">
        <div class="d-flex justify-content-between">
          <span><strong>Car:</strong> <%= booking.carId?.brand %> <%= booking.carId?.name %> (<%= booking.carId?.plateNumber || 'N/A' %>)</span>
          <span><strong>Renter:</strong> <%= booking.userId?.FirstName %> <%= booking.userId?.LastName %></span>
        </div>
        <div class="text-muted">
          Start: <%= booking.rentStart?.toISOString().split('T')[0] %> |
          End: <%= booking.rentEnd?.toISOString().split('T')[0] %> |
          Price: $<%= booking.price %> |
          Booking ID: <%= booking._id %>
        </div>
      </div>
    <% }) %>
  </div>
</div>

</body>
</html>
