<!-- Cookie Consent Banner - NileWay Rentals -->
<link rel="stylesheet" href="/css/cookies.css">

<!-- Cookie Consent Banner HTML will be dynamically generated by JavaScript -->

<script src="/js/cookies.js"></script>

<!-- Optional: Custom event listener for cookie consent changes -->
<script>
    // Listen for cookie consent changes
    window.addEventListener('cookieConsentApplied', function(event) {
        const consent = event.detail;
        console.log('Cookie consent applied:', consent);
        
        // You can add custom logic here based on consent
        // For example:
        
        // Enable/disable language switching based on functional cookies
        if (consent.functional) {
            // Allow language cookies
            document.body.classList.add('functional-cookies-enabled');
        } else {
            document.body.classList.remove('functional-cookies-enabled');
        }
        
        // Enable/disable analytics tracking
        if (consent.analytics) {
            // Load analytics scripts
            // loadGoogleAnalytics();
        }
        
        // Enable/disable marketing pixels
        if (consent.marketing) {
            // Load marketing scripts
            // loadFacebookPixel();
        }
    });
    
    // Optional: Function to check consent status anywhere in your app
    function hasConsentFor(category) {
        const consent = window.cookieConsent?.getConsentStatus();
        return consent && consent[category] === true;
    }
    
    // Example usage:
    // if (hasConsentFor('analytics')) {
    //     // Track user behavior
    // }
</script> 